Matrix Toolkit - Production Edition
Build: 042
Build Date: 2026-07-17
Status: Stable

BUILD 042 - ROUTER INFO, LTE AND FLEET REPAIR

Fleet:
- Fixes Node: unknown using direct zerotier-cli parsing.
- Keeps runtime and matrix-zerotier fallbacks.

Diagnostics:
- Reads Hostname from UCI with hostname fallback.
- Uses default-route fallbacks for WAN IP, gateway and interface.
- Uses resolver files when UBUS DNS fields are empty.
- Shows current ZeroTier information.

LTE:
- Installs /usr/local/matrix/lte_info_cache.sh as a root worker.
- Adds one root cron entry every five minutes.
- Runs an immediate cache update during installation.
- Adds a manual Refresh LTE Data action.
- The existing one-second root service processes manual LTE refresh requests.
- Cache includes raw modem output and normalized RSSI, RSRP, RSRQ and SINR.
- Handles multiple common gsmctl output formats.

Router Info:
- Uses UCI hostname first.
- Uses /etc/version for the real RUTOS firmware version.
- Adds direct ZeroTier CLI fallbacks for runtime, status, Node ID and IP.
- Adds WAN route and DNS fallbacks.

Doctor:
- Checks LTE worker, root cron, cache and cache content.
- Checks Diagnostics hostname.
- Checks Router Info and Fleet Node ID.
