Matrix Toolkit - Production Edition
Build: 022
Build Date: 2026-07-13
Status: Stable

BUILD 022 - SELF-HEALING MATRIX DOCTOR

Matrix Doctor:
- Runs diagnostics first.
- Automatically starts ZeroTier self-heal for repairable ZeroTier failures.
- Re-runs all diagnostics after repair.
- Returns success only when the final diagnostic has zero failures.
- Use matrix-doctor --check-only to diagnose without automatic repair.
- Prevents repair loops with an internal no-repair flag.

Recovery:
- Reinstalls the current CLI tools from the release package.
- No longer leaves an old embedded matrix-update or matrix-zerotier command active.
- SAFE_UPDATE skips the broad network restart.
- Proven ZeroTier runtime, identity and parser logic is retained.

Logs:
- /tmp/matrix_doctor_initial.log
- /tmp/matrix_doctor_final.log
- /tmp/matrix_zerotier_state.log
- /usr/local/matrix/logs/matrix_update_last.log
