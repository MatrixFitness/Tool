BUILD 028

Default Admin recovery password:
Matrix2025

Password file:
/usr/local/matrix/admin_password.txt

Required permissions:
owner root:root
mode 644

The default is created only when the file is missing or empty.
Existing custom passwords are preserved.

After update:
matrix-update
matrix-version
matrix-doctor
