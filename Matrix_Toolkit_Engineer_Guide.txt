BUILD 027

Default Admin recovery password:
Matrix2025

The default is used only when:
/usr/local/matrix/admin_password.txt
is missing or empty.

An existing custom password is never overwritten.

After update:
matrix-version
matrix-doctor

Admin:
http://<router-ip>:8081/cgi-bin/admin_login.sh
