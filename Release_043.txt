Matrix Toolkit - Production Edition
Build: 043
Build Date: 2026-07-17
Status: Stable

BUILD 043 - STABILITY RELEASE

Recovery:
- Stops Matrix services before replacement.
- Stages the source outside /usr/local/matrix before cleanup.
- Removes immutable attributes when chattr is available.
- Restores root ownership and write permissions before deletion.
- Aborts immediately if the old runtime cannot be removed.
- Never continues installation over an incomplete old runtime.
- Validates installation and web endpoints before reporting success.

Web Matrix Doctor:
- Replaced the raw CLI CGI with a lightweight queued web interface.
- Root service runs Matrix Doctor.
- Browser displays RUNNING, SUCCESS or ERROR and the captured report.
- Valid HTTP output is always returned; no Bad Gateway.

Homepage:
- ZeroTier IP present means CONNECTED.
- Direct zerotier-cli and identity fallbacks.
- Displays Build 043 instead of vBuild043.

Fleet:
- Consistent Node ID detection using runtime, direct CLI, matrix-zerotier and identity.public.
- Online routers no longer show unknown when an identity exists.

LTE Analyse:
- Redesigned to match the modern Matrix Toolkit appearance.
- Modern header, status cards, signal cards and responsive layout.
- Refresh LTE Data button in the header.
- Raw modem data and reference values are collapsible.
- No automatic refresh.

Retained:
- Build 043 LTE cache and Router Info repairs.
- Build 041 Quick Test WiFi.
- Dynamic Fleet IP configuration.
